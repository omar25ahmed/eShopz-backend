var stripe = Stripe("<%= Rails.application.credentials.stripe[:publishable_key] %>");

stripe.redirectToCheckout({ sessionId: "<%= @session.id %>" }).then(function (result) { console.log(result.error.message); });


